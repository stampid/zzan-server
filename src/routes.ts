const USER = "/user";
const SIGNUP = "/signup";
const EMAILAUTH = "/emailauth";
const EMAILSEND = "/emailsend";

const routes = {
  user: USER,
  signup: SIGNUP,
  emailauth: EMAILAUTH,
  emailsend: EMAILSEND
};

export default routes;
