const USER = "/user";
const SIGNUP = "/signup";
const SIGNIN = "/signin";
const EMAILAUTH = "/emailauth";
const EMAILSEND = "/emailsend";

const routes = {
  user: USER,
  signup: SIGNUP,
  signin: SIGNIN,
  emailauth: EMAILAUTH,
  emailsend: EMAILSEND
};

export default routes;
